Observability in the demonstrator is intentionally light: structured JSON logs (Serilog / Logback) to stdout (`docker compose logs -f`), correlation ids (`X-Correlation-Id`), `/health/live`, `/health/ready`, `/actuator/health`, and the Administrator status page (`/admin`, backed by `GET /api/v1/admin/status`: service pings, solver latency, recent errors). Drop a Prometheus/Grafana or OTEL collector compose fragment here if needed.
